export enum SetupStoreId {
  App = 'app-store',
  Theme = 'theme-store',
  Auth = 'auth-store',
  Route = 'route-store',
  Tab = 'tab-store',
  Log = 'log-store',
  Resource = 'resource'
}

export enum platform {
  android = 1,
  ios = 2
}
